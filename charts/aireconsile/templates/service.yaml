apiVersion: v1
kind: Service
metadata:
  name: aireconsile
  namespace: {{ .Release.Namespace | quote }}
spec:
  selector:
    app: aireconsile
  ports:
    - name: http
      port: 8080
      targetPort: http
